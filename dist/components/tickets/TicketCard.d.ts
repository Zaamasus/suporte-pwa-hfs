import { Ticket } from '../../types';
interface TicketCardProps {
    ticket: Ticket;
}
export declare function TicketCard({ ticket }: TicketCardProps): import("react/jsx-runtime").JSX.Element;
export {};
