import { Ticket } from '../../types';
interface StatsCardsProps {
    tickets: Ticket[];
}
export declare function StatsCards({ tickets }: StatsCardsProps): import("react/jsx-runtime").JSX.Element;
export {};
