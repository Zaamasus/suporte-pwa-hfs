// Este arquivo foi criado para lidar com os erros de console relacionados ao PIN Company Discounts Provider
// Estes erros provavelmente vêm de alguma extensão do navegador e não afetam o funcionamento da aplicação

// Implementação vazia para evitar erros
export const PINCompanyDiscountsProvider = {
  init: () => {
    // Implementação vazia
    return Promise.resolve();
  },
  getDiscounts: () => {
    // Retorna um array vazio para evitar erros
    return Promise.resolve([]);
  }
};

// Exportando um componente vazio por padrão
export default function PINComponent() {
  return null;
} 